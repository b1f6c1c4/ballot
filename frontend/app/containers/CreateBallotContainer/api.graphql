mutation CreateBallot($name: String!) {
  createBallot(input: {
    name: $name,
  }) {
    bId
    name
    status
  }
}

# Hack graphql-tag/loader
# [bug](https://github.com/apollographql/graphql-tag/issues/137)
query Hack { hack }
