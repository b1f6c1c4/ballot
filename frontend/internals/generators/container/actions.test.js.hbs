import * as {{ constantCase name }} from '../constants';

import * as {{ camelCase name }}Actions from '../actions';

describe('{{ properCase name }} actions', () => {
  // Actions
  describe('{{ camelCase actionName }} action', () => {
    it('has a type of {{ constantCase actionName }}_ACTION', () => {
      expect({{ camelCase name }}Actions.{{ camelCase actionName }}().type).toEqual({{ constantCase name }}.{{ constantCase actionName }}_ACTION);
    });
  });
});
