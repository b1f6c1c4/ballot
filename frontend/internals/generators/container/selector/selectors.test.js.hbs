describe('{{ properCase mselectorName }}', () => {
  const selector = {{ camelCase name }}Selectors.{{ properCase mselectorName }}();

  it('should handle null', () => {
    const mockedState = fromJS({});
    expect(selector(mockedState)).not.toEqual(expect.anything());
  });

  it('should select {{ camelCase mselectorName }}', () => {
    const {{ camelCase mselectorName }} = { key: 'value' };
    const state = fromJS({
      {{ camelCase mselectorName }},
    });
    const mockedState = fromJS({
      {{ camelCase name }}: state,
    });
    expect(selector(mockedState)).toEqual({{ camelCase mselectorName }});
  });
});
